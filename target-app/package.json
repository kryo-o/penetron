{
  "name": "penetron-target-app",
  "version": "0.1.0",
  "private": true,
  "description": "Acme Demo Shop — intentionally-vulnerable demo app used as Penetron's pen-test fixture (authorized testing only).",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "seed": "node server/db.js",
    "smoke": "node scripts/smoke.mjs",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "client:dev": "npm --prefix client run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  }
}
